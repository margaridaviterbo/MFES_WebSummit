/*
 * The Transaction class represents a monetary transaction.
 * It's purpose is to mantain a history of transactions so that the users can
 * monitor the financial outcome of the event.
 */
class Transaction
instance variables
	public Amount : int
operations
	public Transaction: int ==> Transaction
	Transaction(amount) == (
		Amount := amount;
		return self
	);
end Transaction